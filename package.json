{
  "name": "payment-ops-prototype-lab",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "turbo run build --concurrency=1",
    "demo": "pnpm run up",
    "dev": "turbo run dev --parallel",
    "dev:brand-runtime": "turbo run dev --parallel --filter=@payment-ops/payment-core --filter=@payment-ops/layout-builder --filter=@payment-ops/builder-frontend",
    "dev:frontend": "pnpm --filter @payment-ops/builder-frontend dev",
    "up": "pnpm run setup && pnpm run dev",
    "up:brand-runtime": "pnpm install --prefer-offline && pnpm run setup:brand-runtime && pnpm run dev:brand-runtime",
    "setup": "pnpm run docker:up && pnpm run prisma:generate && pnpm run db:deploy",
    "setup:brand-runtime": "pnpm run docker:up && pnpm run prisma:generate && pnpm run db:deploy:brand-runtime",
    "prisma:generate": "pnpm --filter @payment-ops/payment-core prisma:generate",
    "db:deploy": "set -a; [ -f .env ] && . ./.env; set +a; pnpm --filter @payment-ops/sms-gateway prisma:deploy && pnpm --filter @payment-ops/receipt-recognizer prisma:deploy && pnpm --filter @payment-ops/layout-builder prisma:deploy && pnpm --filter @payment-ops/payment-core prisma:deploy",
    "db:deploy:brand-runtime": "set -a; [ -f .env ] && . ./.env; set +a; pnpm --filter @payment-ops/layout-builder prisma:deploy && pnpm --filter @payment-ops/payment-core prisma:deploy",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck --concurrency=1",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "devDependencies": {
    "@eslint/js": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:"
  }
}
